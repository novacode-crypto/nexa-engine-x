import { Logger } from './logger';

export class TrayManager {
  constructor(private logger: Logger) {}

  // Gestión adicional de la bandeja si es necesaria
  // La creación principal está en main.ts
}